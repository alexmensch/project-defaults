# CLAUDE.md

Guidance for Claude Code when working in this repo. Replace placeholders
(`{{...}}`) with project specifics; delete sections that don't apply.

## Build / dev commands

```bash
{{INSTALL_COMMAND}}        # e.g. pnpm install
{{BUILD_COMMAND}}          # e.g. pnpm run build
{{TEST_COMMAND}}           # e.g. pnpm test
{{LINT_COMMAND}}           # e.g. pnpm run lint
{{DEV_COMMAND}}            # e.g. pnpm run dev or pnpm run 11ty:watch
```

## Architecture

{{One-paragraph summary of what this project is and how it's structured.}}

### Key files

- `{{PATH}}` — {{purpose}}
- `{{PATH}}` — {{purpose}}

### Directory layout

- `src/` — {{...}}
- `tests/` — {{...}}

## Conventions

{{List repo-specific patterns Claude should follow: naming, code style choices
not enforced by linters, where to add new files, etc. Keep terse — Claude
re-reads this file every session.}}

## Session Completion

When ending a work session, complete ALL steps below. Work is NOT complete
until `git push` succeeds.

1. **File issues for remaining work** — bead/issue for anything that needs
   follow-up.
2. **Run quality gates** (if code changed) — tests, linters, builds.
3. **Update issue status** — close finished work, update in-progress items.
4. **Push to remote** — this is MANDATORY:
   ```bash
   git pull --rebase
   git push
   git status   # MUST show "up to date with origin"
   ```
   For bd projects, `.beads/issues.jsonl` propagates via the normal
   `git push` (no `bd dolt push` needed unless a Dolt remote is configured —
   see the `bd-persistence` memory).
5. **Clean up** — clear stashes; `git fetch --prune` then `git worktree
   remove` and `git branch -D` for the merged branch. Repos have
   auto-delete-on-merge enabled, so don't run `git push --delete`.
6. **Verify** — all changes committed AND pushed.
7. **Hand off** — provide context for the next session if more work remains.

**Critical rules:**

- Work is NOT complete until `git push` succeeds.
- NEVER stop before pushing — that leaves work stranded locally.
- NEVER say "ready to push when you are" — YOU must push.
- If push fails, resolve and retry until it succeeds.

<!-- The bd integration block below is appended automatically by
     `bd hooks integrate`. Do not edit between the BEGIN/END markers. -->
