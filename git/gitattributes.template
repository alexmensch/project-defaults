# Git LFS rules — uncomment / extend as needed for the project.
# Run `git lfs install` once per machine and `git lfs track "<pattern>"`
# (or just edit this file) to enroll patterns.

# Audio
# *.mp3 filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
# *.flac filter=lfs diff=lfs merge=lfs -text

# Video
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# *.mov filter=lfs diff=lfs merge=lfs -text

# Large images / raw
# *.psd filter=lfs diff=lfs merge=lfs -text
# *.tiff filter=lfs diff=lfs merge=lfs -text
# *.raw filter=lfs diff=lfs merge=lfs -text

# Specific large binaries — list by path
# path/to/large-file.pdf filter=lfs diff=lfs merge=lfs -text
